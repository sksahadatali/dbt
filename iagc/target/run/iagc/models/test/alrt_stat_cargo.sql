
  create or replace   view GDP_SANDBOX.STAGING.alrt_stat_cargo
  
   as (
    WITH ALRT_STAT_CARGO
AS
(SELECT * FROM GDP_SANDBOX.STAGING.API_EXTERNALSERVICEENTRY_SUBMITBOOKING_SAMPLE)
SELECT LINK,ID FROM ALRT_STAT_CARGO
  );

